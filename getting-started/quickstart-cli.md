# Quickstart: CLI

Install the `infraudit` CLI, log in, connect a cloud provider, and run your first drift scan without leaving the terminal.

_Coming soon._

Until then, follow [CLI installation](../cli/installation.md), then [CLI configuration](../cli/configuration.md), and use the Quick Start section of the [CLI overview](../cli/README.md).
